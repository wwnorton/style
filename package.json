{
  "workspaces": [
    "packages/*",
    "!packages/commitlint-config-norton",
    "!packages/prettier-config-norton"
  ],
  "scripts": {
    "eslint": "eslint . --ignore-path .gitignore",
    "test": "vitest"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^6.1.0",
    "eslint": "^8.43.0",
    "husky": "^8.0.3",
    "lerna": "^7.1.1",
    "lint-staged": "^13.2.3",
    "react": "^18.2.0",
    "typescript": "^5.1.5",
    "vitest": "^0.32.2"
  },
  "engines": {
    "node": ">= 16.13.0"
  }
}
